export enum ModuleRoutes {
  Others = '**',
  Root = '',
  Auth = 'auth',
  User = 'user',
}

export enum AuthRoutes {
  Root = '',
  SignIn = 'sign-in',
}

export enum RoutePathMatchOptions {
  Full = 'full',
}
